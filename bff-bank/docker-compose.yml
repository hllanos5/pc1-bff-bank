version: '3.7'
services:
  bff-bank:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 8080:8080
    networks:
      - servicio_ntt
networks:
  servicio_ntt:
    external: true